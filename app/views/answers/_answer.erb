
<div class='container'>
  <div class="col-sm-2">
      <a href='/answers/<%= response.id %>/votes/add'>
    <h2><span class="glyphicon glyphicon-chevron-up" aria-hidden="true"></span></h2>
    </a)
    <h2><%= response.votes.sum(:vote_value)%></h2>
       <a href='/answers/<%= response.id %>/votes/minus'>
    <h2><span class="glyphicon glyphicon-chevron-down" aria-hidden="true"></span></h2>
  </a>

      votes
    </div>
    <div class="col-sm-10">
      <p><%= response.body %>
      </p>
      <span class="glyphicon glyphicon-user" aria-hidden="true"></span>
      Asked <%= response.created_at.month.to_s + '/' + response.created_at.day.to_s + '/' + response.created_at.year.to_s%> BY
      <%= response.user.first_name + ' ' + response.user.last_name%>
    </div>

  </div>
